export {
	extend,
	min,
	minBy,
	map,
	find,
	filter,
	isUndefined,
	isObject,
	isString,
	isFunction,
	mapValues,
	toArray
} from 'lodash-es';

export function isDefined(value) {
	return value !== undefined;
}